{
  "name": "github",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @homebase/web build",
    "dev": "pnpm --filter @homebase/web dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
