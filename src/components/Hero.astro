---
import Icon from 'astro-icon';
---
<section class="text-center py-16 px-8">
  <div class="max-w-3xl mx-auto">
    <h1 class="text-5xl md:text-7xl font-bold mb-4 bg-clip-text text-transparent bg-gradient-to-r from-purple-400 to-pink-600">
      The Future of Web Development is Here
    </h1>
    <p class="text-lg md:text-xl text-gray-400 max-w-2xl mx-auto mb-8">
      Build faster, more performant websites with Astro's next-gen architecture. Zero JavaScript by default, and a universe of integrations.
    </p>
    <div class="flex justify-center mb-8">
        <a href="/studio" class="bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-8 rounded-full transition-all duration-300 transform hover:scale-105 shadow-lg hover:shadow-2xl">
        Get Started
        </a>
    </div>

    <div class="mt-12 max-w-xl mx-auto">
      <div class="relative">
        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
          <Icon name="mdi:magnify" class="text-gray-400 w-5 h-5" />
        </div>
        <input
          type="text"
          placeholder="What are you looking for?"
          class="w-full bg-gray-800 bg-opacity-50 border border-gray-700 rounded-full py-3 pl-10 pr-10 text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-purple-500 focus:border-transparent transition-all duration-300"
        />
        <div class="absolute inset-y-0 right-0 pr-3 flex items-center">
          <Icon name="mdi:microphone" class="text-gray-400 w-5 h-5 hover:text-white cursor-pointer" />
        </div>
      </div>
    </div>

  </div>
</section>
