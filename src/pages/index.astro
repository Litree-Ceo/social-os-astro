---
import Layout from '../layouts/Layout.astro';
import { Icon } from 'astro-icon/components';
---

<Layout>
  <section class="hero">
    <div class="hero-content">
      <h1 class="hero-title">The Future of Web Development is Here</h1>
      <p class="hero-subtitle">Build faster, more performant websites with Astro's next-gen architecture. Zero JavaScript by default, and a universe of integrations.</p>
      <a href="/studio" class="hero-cta">Get Started</a>
    </div>
  </section>

  <section class="features">
    <div class="feature-grid">
      <div class="card">
        <div class="card-icon">
          <Icon name="mdi:rocket-launch" />
        </div>
        <h3 class="card-title">Blazing Fast</h3>
        <p class="card-description">Astro pages are pre-rendered to static HTML, resulting in the fastest possible load times.</p>
      </div>
      <div class="card">
        <div class="card-icon">
          <Icon name="mdi:island" />
        </div>
        <h3 class="card-title">Islands Architecture</h3>
        <p class="card-description">Ship zero JavaScript by default. Opt-in to client-side interactivity only when you need it.</p>
      </div>
      <div class="card">
        <div class="card-icon">
          <Icon name="mdi:puzzle" />
        </div>
        <h3 class="card-title">UI Framework Agnostic</h3>
        <p class="card-description">Use your favorite frameworks like React, Vue, Svelte, and more, all in the same project.</p>
      </div>
    </div>
  </section>
</Layout>

<style>
  .hero {
    text-align: center;
    padding: 4rem 2rem;
  }

  .hero-title {
    font-family: var(--font-secondary);
    font-size: 3.5rem;
    font-weight: 600;
    margin-bottom: 1rem;
    line-height: 1.2;
  }

  .hero-subtitle {
    color: var(--color-text-secondary);
    font-size: 1.2rem;
    max-width: 600px;
    margin: 0 auto 2rem;
  }

  .hero-cta {
    background-color: var(--color-accent);
    color: var(--color-primary);
    font-weight: 600;
    padding: 0.8rem 2rem;
    border-radius: 8px;
    text-decoration: none;
    transition: background-color 0.3s, box-shadow 0.3s;
  }

  .hero-cta:hover {
    background-color: #A062F7;
    box-shadow: 0 0 15px var(--color-accent-glow);
  }

  .features {
    padding: 4rem 2rem;
  }

  .feature-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .card {
    text-align: center;
    padding: 2.5rem;
  }

  .card-icon {
    font-size: 3rem;
    color: var(--color-accent);
    margin-bottom: 1.5rem;
  }

  .card-title {
    font-family: var(--font-secondary);
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .card-description {
    color: var(--color-text-secondary);
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1rem;
    }
  }
</style>
